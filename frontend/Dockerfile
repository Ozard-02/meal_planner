# frontend is served by backend container (see backend/Dockerfile)
# kept for compatibility — no separate build needed
# ponytail: single container serves static frontend, no npm build chain
FROM scratch
